--- a/src/kernel_src_build_env.sh	2025-08-22 02:18:09.000000000 +0200
+++ b/src/kernel_src_build_env.sh	2025-09-23 14:45:11.138351512 +0200
@@ -32,4 +32,4 @@
 KERNEL_SRC_DIR="kernel-noble"
 KERNEL_DEF_CONFIG="defconfig"
-OOT_SOURCE_LIST="nvethernetrm  nvgpu  nvidia-oot hwpm hardware nvdisplay build/nvidia-public/devicetree unifiedgpudisp"
+OOT_SOURCE_LIST="nvethernetrm  nvgpu  nvidia-oot hwpm hardware nvdisplay build/nvidia-public/devicetree unifiedgpudisp kernel/stereolabs"
 KERNEL_MODULAR_BUILD="y"
--- a/src/Makefile	2025-08-22 02:18:09.000000000 +0200
+++ b/src/Makefile	2025-09-24 15:48:43.194683356 +0200
@@ -7,6 +7,7 @@
 MAKEFILE_DIR := $(abspath $(shell dirname $(lastword $(MAKEFILE_LIST))))
 NVIDIA_CONFTEST ?= $(MAKEFILE_DIR)/out/nvidia-conftest
 NVIDIA_DTS_BUILD_SCRIPTS ?= $(realpath $(MAKEFILE_DIR)/build/nvidia-public/devicetree)
+SL_DRIVERS ?= ${MAKEFILE_DIR}/kernel/stereolabs/
 
 ifdef OE_BUILD
   LD_BFD := $(LD)
@@ -44,12 +45,14 @@
 	@echo   "================================================================================"
 	@echo   "Usage:"
 	@echo   "   make modules          # to build NVIDIA OOT and display drivers"
+	@echo   "   make modules_oot      # to build SL OOT"
 	@echo   "   make dtbs             # to build NVIDIA DTBs"
 	@echo   "   make modules_install  # to install drivers to the INSTALL_MOD_PATH"
 	@echo   "   make clean            # to make clean driver sources"
 	@echo   "================================================================================"
 
-modules: hwpm nvidia-oot nvgpu $(NVIDIA_DISPLAY_MODULE_TARGETS)
+modules: hwpm nvidia-oot stereolabs nvgpu nvidia-display $(NVIDIA_DISPLAY_MODULE_TARGETS)
+modules_oot: stereolabs
 dtbs: nvidia-dtbs
 modules_install: hwpm nvidia-oot nvgpu \
 	$(NVIDIA_DISPLAY_MODULE_INSTALL_TARGETS)
@@ -75,6 +78,7 @@
 endif
 
 hwpm: conftest
+ifneq ($(MAKECMDGOALS), modules_oot)
 	@if [ ! -d "$(MAKEFILE_DIR)/hwpm" ] ; then \
 		echo "Directory hwpm is not found, exiting.."; \
 		false; \
@@ -87,8 +91,10 @@
 		srctree.hwpm=$(MAKEFILE_DIR)/hwpm \
 		srctree.nvconftest=$(NVIDIA_CONFTEST) \
 		$(MAKECMDGOALS)
+endif
 
 nvidia-oot: conftest hwpm
+ifneq ($(MAKECMDGOALS), modules_oot)
 	@if [ ! -d "$(MAKEFILE_DIR)/nvidia-oot" ] ; then \
 		echo "Directory nvidia-oot is not found, exiting.."; \
 		false; \
@@ -107,5 +113,47 @@
 		system_type=l4t \
 		KBUILD_EXTRA_SYMBOLS=$(MAKEFILE_DIR)/hwpm/drivers/tegra/hwpm/Module.symvers \
 		$(MAKECMDGOALS)
+endif
+
+stereolabs: nvidia-oot
+ifneq ($(MAKECMDGOALS), modules_oot)
+	@if [ ! -d "$(MAKEFILE_DIR)/kernel/stereolabs" ] ; then \
+		echo "Directory stereolabs is not found, exiting.."; \
+		false; \
+	fi
+	@echo   "================================================================================"
+	@echo   "make $(MAKECMDGOALS) - stereolabs ..."
+	@echo   "================================================================================"
+	$(MAKE) -j $(NPROC) ARCH=arm64 \
+		-C $(KERNEL_OUTPUT) \
+		M=$(MAKEFILE_DIR)/kernel/stereolabs \
+		CONFIG_TEGRA_OOT_MODULE=m \
+		EXTRA_CFLAGS="-I$(MAKEFILE_DIR)/nvidia-oot/include -I$(NVIDIA_CONFTEST)" \
+		srctree.sl_drivers=$(SL_DRIVERS) \
+		srctree.nvidia-oot=$(MAKEFILE_DIR)/nvidia-oot \
+		srctree.nvconftest=$(NVIDIA_CONFTEST) \
+		KBUILD_EXTRA_SYMBOLS="$(MAKEFILE_DIR)/hwpm/drivers/tegra/hwpm/Module.symvers \
+		                      $(MAKEFILE_DIR)/nvidia-oot/Module.symvers" \
+		$(MAKECMDGOALS)
+else
+	@if [ ! -d "$(MAKEFILE_DIR)/kernel/stereolabs" ] ; then \
+		echo "Directory stereolabs is not found, exiting.."; \
+		false; \
+	fi
+	@echo   "================================================================================"
+	@echo   "make $(MAKECMDGOALS) - stereolabs ..."
+	@echo   "================================================================================"
+	$(MAKE) -j $(NPROC) ARCH=arm64 \
+		-C $(KERNEL_HEADERS) \
+		M=$(MAKEFILE_DIR)/kernel/stereolabs \
+		CONFIG_TEGRA_OOT_MODULE=m \
+		EXTRA_CFLAGS="-I$(MAKEFILE_DIR)/nvidia-oot/include -I$(NVIDIA_CONFTEST)" \
+		srctree.sl_drivers=$(SL_DRIVERS) \
+		srctree.nvidia-oot=$(MAKEFILE_DIR)/nvidia-oot \
+		srctree.nvconftest=$(NVIDIA_CONFTEST) \
+		KBUILD_EXTRA_SYMBOLS="$(MAKEFILE_DIR)/hwpm/drivers/tegra/hwpm/Module.symvers \
+		                      $(MAKEFILE_DIR)/nvidia-oot/Module.symvers" \
+		modules
+endif
 
 nvgpu: conftest nvidia-oot
--- a/src/nvbuild.sh	2025-08-22 02:18:09.000000000 +0200
+++ b/src/nvbuild.sh	2025-09-23 17:50:04.466912190 +0200
@@ -49,6 +49,7 @@
         -h              Displays this help
         -r              Enable RT Kernel
         -m              Build/install NVIDIA OOT modules only (skip kernel)
+        -mr             Build/install SL OOT set of modules only (skip kernel/display/nv-oot/...)
         -i              Perform install to INSTALL_MOD_PATH
         -o <outdir>     Creates kernel build output in <outdir>
 EOM
@@ -74,6 +75,10 @@
 				OOT_MODULES_ONLY=1
 				shift 1
 				;;
+			-mr)
+				REDUCED_OOT_MODULES_ONLY=1
+				shift 1
+				;;
 			-i)
 				DO_INSTALL=1
 				shift 1
@@ -97,7 +102,7 @@
 
 	# Sync kernel source directory to output dir
 	echo "Syncing kernel/${KERNEL_SRC_DIR}"
-	rsync -a --delete "kernel/${KERNEL_SRC_DIR}" "${KERNEL_OUT_DIR}/kernel/"
+	rsync -a "kernel/${KERNEL_SRC_DIR}" "${KERNEL_OUT_DIR}/kernel/"
 	cp -a kernel/Makefile "${KERNEL_OUT_DIR}/kernel/"
 }
 
@@ -116,7 +121,7 @@
 	for list in ${OOT_SOURCE_LIST}
 	do
 		echo "Syncing ${list}"
-		rsync -aR --delete "${list}" "${KERNEL_OUT_DIR}"
+		rsync -aR "${list}" "${KERNEL_OUT_DIR}"
 	done
 	cp -a Makefile "${KERNEL_OUT_DIR}/"
 }
@@ -141,8 +146,12 @@
 	export KERNEL_SRC_DIR="${KERNEL_SRC_DIR}"
 	export KERNEL_DEF_CONFIG="${KERNEL_DEF_CONFIG}"
 
-	echo "Building kernel sources in ${kernel_out_dir}"
-	make -C "${kernel_out_dir}"
+	if [ "${REDUCED_OOT_MODULES_ONLY}" -eq 1 ]; then
+		echo "Skipping the kernel build, just testing it was compiled once"
+	else
+		echo "Building kernel sources in ${kernel_out_dir}"
+		make -C "${kernel_out_dir}"
+	fi
 
 	image="${kernel_out_dir}/${KERNEL_SRC_DIR}/arch/arm64/boot/Image"
 	if [ ! -f "${image}" ]; then
@@ -167,7 +176,13 @@
 	echo "Building NVIDIA OOT modules sources in ${KERNEL_OUT_DIR}"
 	echo "Using KERNEL_HEADERS from directory : ${KERNEL_HEADERS}"
 	# shellcheck disable=SC2154
-	make -C "${KERNEL_OUT_DIR}" kernel_name="${kernel_name}" system_type=l4t modules
+	if [ "${REDUCED_OOT_MODULES_ONLY}" -eq 1 ]; then
+		make -C "${KERNEL_OUT_DIR}" kernel_name="${kernel_name}" system_type=l4t modules_oot
+	else
+		make -C "${KERNEL_OUT_DIR}" kernel_name="${kernel_name}" system_type=l4t modules
+	fi
+
+	touch "${KERNEL_OUT_DIR}"/.BUILD_ONCE
 
 	echo "Building NVIDIA DTBs"
 	make -C "${KERNEL_OUT_DIR}" dtbs
@@ -202,7 +214,7 @@
 			echo "Error: Path ${CROSS_COMPILE}gcc does not exist."
 			exit 1
 		fi
-		if [ "${OOT_MODULES_ONLY}" -eq 1 ]; then
+		if [[ "${OOT_MODULES_ONLY}" -eq 1 || "${REDUCED_OOT_MODULES_ONLY}" -eq 1 ]]; then
 			if [ -z "${KERNEL_HEADERS}" ]; then
 				echo "Error: Env variable KERNEL_HEADERS is not set!!"
 				exit 1
